import { RetroWindow } from '@/components/retro-window';

export default function RolesPage() {
  return (
    <RetroWindow title="sys://roles">
      <p className="font-body text-muted-foreground text-sm">
        Role categories -- coming in Step 10
      </p>
    </RetroWindow>
  );
}
