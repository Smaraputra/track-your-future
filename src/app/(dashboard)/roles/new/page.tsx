import { RetroWindow } from '@/components/retro-window';

export default function NewRolePage() {
  return (
    <RetroWindow title="sys://roles/new">
      <p className="font-body text-muted-foreground text-sm">
        New role category -- coming in Step 10
      </p>
    </RetroWindow>
  );
}
