import { RetroWindow } from '@/components/retro-window';

export default function AnalyticsPage() {
  return (
    <RetroWindow title="sys://analytics">
      <p className="font-body text-muted-foreground text-sm">
        Analytics -- coming in Step 14
      </p>
    </RetroWindow>
  );
}
