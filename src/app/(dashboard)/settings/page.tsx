import { RetroWindow } from '@/components/retro-window';

export default function SettingsPage() {
  return (
    <RetroWindow title="sys://settings">
      <p className="font-body text-muted-foreground text-sm">
        Settings -- coming in Step 15
      </p>
    </RetroWindow>
  );
}
