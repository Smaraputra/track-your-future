import { RetroWindow } from '@/components/retro-window';

export default function DocumentsPage() {
  return (
    <RetroWindow title="sys://documents">
      <p className="font-body text-muted-foreground text-sm">
        Documents -- coming in Step 12
      </p>
    </RetroWindow>
  );
}
